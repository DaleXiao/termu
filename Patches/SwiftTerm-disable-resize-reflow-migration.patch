diff --git a/Sources/SwiftTerm/Buffer.swift b/Sources/SwiftTerm/Buffer.swift
index 8b6cfab..5710bbe 100644
--- a/Sources/SwiftTerm/Buffer.swift
+++ b/Sources/SwiftTerm/Buffer.swift
@@ -405,3 +405,2 @@ public var isReflowEnabled: Bool {
-        // termu reflows only when wrapped lines are full-width scrollback rows.
-        let hasFullWidthMargins = !marginMode || (_marginLeft == 0 && _marginRight == _cols - 1)
-        return hasScrollback && hasFullWidthMargins
+        // termu disables resize reflow because repeated width changes can duplicate wrapped scrollback lines.
+        return false
