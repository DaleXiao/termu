diff --git a/Sources/SwiftTerm/Buffer.swift b/Sources/SwiftTerm/Buffer.swift
index e7e7857..8b6cfab 100644
--- a/Sources/SwiftTerm/Buffer.swift
+++ b/Sources/SwiftTerm/Buffer.swift
@@ -402,7 +402,9 @@ public final class Buffer {
     }
     
     public var isReflowEnabled: Bool {
-        return hasScrollback
+        // termu reflows only when wrapped lines are full-width scrollback rows.
+        let hasFullWidthMargins = !marginMode || (_marginLeft == 0 && _marginRight == _cols - 1)
+        return hasScrollback && hasFullWidthMargins
     }
     
     public func resize (newCols : Int, newRows : Int)
@@ -501,11 +502,11 @@ public final class Buffer {
         
         if isReflowEnabled {
             reflow (newCols, newRows)
-            // Trim the end of the line off if cols shrunk
-            if cols > newCols {
-                for i in 0..<lines.maxLength {
-                    lines [i].resize (cols: newCols, fillData: CharData.Null)
-                }
+        }
+        // Trim the end of the line off if cols shrunk
+        if cols > newCols {
+            for i in 0..<lines.maxLength {
+                lines [i].resize (cols: newCols, fillData: CharData.Null)
             }
         }
         
